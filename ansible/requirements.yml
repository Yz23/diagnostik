---
# Installer avec : ansible-galaxy collection install -r ansible/requirements.yml
collections:
  - name: ansible.posix
    version: ">=1.5"
  - name: community.general
    version: ">=8.0"
  - name: google.cloud
    version: ">=1.3"       # inventaire dynamique GCP
  - name: amazon.aws
    version: ">=7.0"       # inventaire dynamique AWS
  - name: azure.azcollection
    version: ">=1.19"      # inventaire dynamique Azure
