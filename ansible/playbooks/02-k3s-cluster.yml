---
- name: Install k3s server
  hosts: k3s_server
  become: true
  roles:
    - k3s-server

- name: Install k3s agents
  hosts: k3s_agents
  become: true
  roles:
    - k3s-agent

- name: Configure node labels and taints
  hosts: k3s_server
  become: true
  roles:
    - k8s-node-config
