# Roadmap

## POC

- API, mock connector, file connector scaffold, pipeline YAML, mock embeddings, OpenSearch templates, text/hybrid search, Streamlit UI, Docker Compose.

## MVP

- Workspaces, auth, PostgreSQL metadata, Kafka workers, API/RSS/DB connectors, robust pipeline engine, monitoring, plugin SDK, contributor docs.

## Production

- Kubernetes app manifests, RBAC, OIDC, External Secrets, backups, HA, security CI/CD, observability, tenant isolation, community marketplace.

## Private Use Cases

- Private repository, private plugins, private connectors, private datasets, private indexes, specialized models, private dashboards.
