apiVersion: v1
kind: Secret
metadata:
  name: kafka-credentials
  namespace: data-platform
  labels:
    app: kafka
type: Opaque
stringData:
  # Replace in production with your secret manager or sealed secret flow.
  KAFKA_ADMIN_PASSWORD: ""
  KAFKA_CLIENT_PASSWORD: ""
