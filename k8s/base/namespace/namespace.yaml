apiVersion: v1
kind: Namespace
metadata:
  name: data-platform
  labels:
    app.kubernetes.io/managed-by: kustomize
    # ── Pod Security Admission (K8s 1.25+) ────────────────────────────────────
    # warn : signale sans bloquer (migration en douceur)
    # enforce: baseline bloque les pods les plus dangereux (pas privileged sauf sysctl init)
    # NOTE: OpenSearch sysctl initContainers require privileged: true.
    #        To block them, switch to enforce: restricted + use pod-level sysctls.
    #        (requires the node to allow the non-secure sysctl vm.max_map_count)
    pod-security.kubernetes.io/warn: baseline
    pod-security.kubernetes.io/warn-version: "v1.30"
    pod-security.kubernetes.io/audit: baseline
    pod-security.kubernetes.io/audit-version: "v1.30"
