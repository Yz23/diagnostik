apiVersion: v1
kind: Service
metadata:
  name: diagnostik-ui
  namespace: data-platform
  labels:
    app: diagnostik-ui
spec:
  type: ClusterIP
  selector:
    app: diagnostik-ui
  ports:
    - name: http
      port: 8501
      targetPort: http
