# ResourceQuota + LimitRange — protection against unbounded workloads.
# Without these objects, a pod without limits can consume all node RAM.

apiVersion: v1
kind: ResourceQuota
metadata:
  name: data-platform-quota
  namespace: data-platform
spec:
  hard:
    # Pods
    pods:                    "60"
    # CPU
    requests.cpu:            "24"
    limits.cpu:              "48"
    # Memory
    requests.memory:         "80Gi"
    limits.memory:           "128Gi"
    # Stockage
    persistentvolumeclaims:  "30"
    requests.storage:        "2Ti"
    # Services
    services:                "20"
    services.loadbalancers:  "2"
    # ConfigMaps / Secrets
    configmaps:              "30"
    secrets:                 "20"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: data-platform-limits
  namespace: data-platform
spec:
  limits:
    # Defaults for containers without explicit requests/limits
    - type: Container
      default:
        cpu:    "500m"
        memory: "512Mi"
      defaultRequest:
        cpu:    "100m"
        memory: "128Mi"
      max:
        cpu:    "8"
        memory: "16Gi"
      min:
        cpu:    "50m"
        memory: "64Mi"
    # Limit for PVCs (avoid accidentally large volumes)
    - type: PersistentVolumeClaim
      max:
        storage: "600Gi"
      min:
        storage: "1Gi"
