apiVersion: v1
kind: ConfigMap
metadata:
  name: opensearch-dashboards-config
  namespace: data-platform
  labels:
    app: opensearch-dashboards
data:
  opensearch_dashboards.yml: |
    server.name: opensearch-dashboards
    server.host: "0.0.0.0"
    server.port: 5601

    # FIX C1 : utiliser HTTPS vers OpenSearch (après activation TLS côté OS)
    # Remplacer http:// par https:// une fois les certs en place
    opensearch.hosts:
      - "https://opensearch:9200"

    # FIX C1 : référencer la CA pour valider le certificat OpenSearch
    opensearch.ssl.certificateAuthorities:
      - /usr/share/opensearch-dashboards/config/certs/ca.crt
    opensearch.ssl.verificationMode: full

    opensearch.username: "admin"
    opensearch.password: "${OPENSEARCH_PASSWORD}"
    opensearch.requestHeadersAllowlist:
      - authorization
      - securitytenant

    opensearch_security.multitenancy.enabled: true
    opensearch_security.multitenancy.tenants.preferred:
      - Private
      - Global
    opensearch_security.readonly_mode.roles:
      - kibana_read_only

    # FIX M1 : cookie.secure DOIT être true quand TLS est activé sur l'Ingress
    # Passer à false uniquement en dev local sans TLS
    opensearch_security.cookie.secure: true

    logging.verbose: false
