apiVersion: v1
kind: Secret
metadata:
  name: diagnostik-api-secrets
  namespace: data-platform
  labels:
    app: diagnostik-api
type: Opaque
stringData:
  # Replace with sealed secrets, External Secrets, or your secret manager.
  DIAGNOSTIK_API_KEY: ""
