# TEMPLATE ONLY — no real values committed.
# Create the real secret with: bash scripts/create-secrets.sh
apiVersion: v1
kind: Secret
metadata:
  name: opensearch-credentials
  namespace: data-platform
type: Opaque
stringData:
  OPENSEARCH_INITIAL_ADMIN_PASSWORD: "CHANGE_ME"
