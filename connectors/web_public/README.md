# Web Public Connector

Scaffold for compliant public web fetching.

Rules:

- Respect `robots.txt`.
- Use an explicit user agent.
- Apply rate limits.
- Do not bypass login, paywalls, CAPTCHAs, or access controls.
- Do not implement aggressive crawling.
