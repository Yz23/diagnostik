# API Generic Connector

Scaffold for simple HTTP API ingestion with explicit URL, headers from secrets, pagination, retries, and rate limits.

Do not commit API tokens or private endpoints in connector configs.
