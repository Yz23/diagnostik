# Mock Connector

Generates synthetic community-safe documents for demos and tests.

It does not call external services, does not require secrets, and does not model any private or sensitive use case.

Example config:

```yaml
connector: mock
dataset_id: demo
count: 12
domain: cybersecurity
language: en
```
