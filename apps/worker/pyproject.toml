[project]
name = "diagnostik-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["diagnostik-common"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
